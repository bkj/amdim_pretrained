#!/usr/bin/env/python

from setuptools import setup, find_packages

VERSION = '0.0'

setup(
    name='rsp',
    author='Ben Johnson',
    author_email='ben@canfield.io',
    classifiers=[],
    description='rsp',
    keywords=['rsp'],
    license='MIT',
    packages=find_packages(),
    version=VERSION
)